# auto-generated by grc.converter

id: lfast_quad_demod_volk
label: Quadrature Demod Volk
category: '[LFAST Accelerated Blocks]'

parameters:
-   id: gain
    label: Gain
    dtype: real
    default: samp_rate/(2*math.pi*fsk_deviation_hz/8.0)

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import lfast
    make: lfast.quad_demod_volk(${gain})
    callbacks:
    - set_gain(${gain})

file_format: 1
