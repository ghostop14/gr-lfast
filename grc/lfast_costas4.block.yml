# auto-generated by grc.converter

id: lfast_costas4
label: Accel Costas 4th Order Loop
category: '[LFAST Accelerated Blocks]'

parameters:
-   id: w
    label: Loop Bandwidth
    dtype: real
-   id: genSignalPDUs
    label: Gen Signal PDUs
    dtype: enum
    options: ['False', 'True']
    option_labels: ['Off', 'On']

inputs:
-   domain: stream
    dtype: complex
-   domain: message
    id: msgin
    optional: true

outputs:
-   domain: stream
    dtype: complex
-   domain: message
    id: msgout
    optional: true

templates:
    imports: import lfast
    make: lfast.costas4(${w}, 4, ${genSignalPDUs})
    callbacks:
    - set_loop_bandwidth(${w})

file_format: 1
