# auto-generated by grc.converter

id: lfast_MTFIRFilterXX
label: Multithreaded FIR Filter
category: '[LFAST Accelerated Blocks]'

parameters:
-   id: type
    label: Type
    dtype: enum
    options: [CCF, CCC, FF]
    option_labels: [Complex->Complex (Real Taps), Complex->Complex (Complex Taps),
        Float->Float (Real Taps)]
    option_attributes:
        input: [complex, complex, float]
        output: [complex, complex, float]
        taps: [real_vector, real_vector, real_vector]
    hide: part
-   id: nthreads
    label: Threads
    dtype: int
    default: '4'
-   id: taps
    label: Taps
    dtype: ${ type.taps }

inputs:
-   domain: stream
    dtype: ${ type.input }

outputs:
-   domain: stream
    dtype: ${ type.output }

templates:
    imports: import lfast
    make: lfast.MTFIRFilter${type}(1, ${taps}, ${nthreads})
    callbacks:
    - set_taps(${taps})

file_format: 1
