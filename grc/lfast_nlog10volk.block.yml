# auto-generated by grc.converter

id: lfast_nlog10volk
label: Log10 Volk
category: '[LFAST Accelerated Blocks]'

parameters:
-   id: n
    label: n
    dtype: real
    default: '1'
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: k
    label: k
    dtype: real
    default: '0'
-   id: vlen
    label: Vec Length
    dtype: int
    default: '1'
    hide: ${ 'part' if vlen == 1 else 'none' }

inputs:
-   domain: stream
    dtype: float
    vlen: ${ vlen }

outputs:
-   domain: stream
    dtype: float
    vlen: ${ vlen }
asserts:
- ${ vlen >= 1 }

templates:
    imports: import lfast
    make: lfast.nlog10volk(${n}, ${vlen}, ${k})

file_format: 1
