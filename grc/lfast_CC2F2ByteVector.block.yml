# auto-generated by grc.converter

id: lfast_CC2F2ByteVector
label: Accel Complex2Real2Byte2Vector
category: '[LFAST Accelerated Blocks]'

parameters:
-   id: scale
    label: Scale
    dtype: int
    default: '1'
-   id: vecLength
    label: Vector length
    dtype: int
    default: '1'
    hide: ${ 'part' if vecLength == 1 else 'none' }
-   id: num_items
    label: Num Items
    dtype: int
    default: '2'
    hide: ${ 'part' if vecLength == 1 else 'none' }

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ vecLength }

outputs:
-   domain: stream
    dtype: byte
    vlen: ${ vecLength*num_items }
asserts:
- ${ num_items > 0 }
- ${ vecLength >= 1 }

templates:
    imports: import lfast
    make: lfast.CC2F2ByteVector(${scale}, ${vecLength}, ${num_items})

file_format: 1
